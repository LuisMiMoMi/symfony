{% extends 'base.html.twig' %}
{% block title %}Fitxa llibre{% endblock %}
{% block body %}
    <h1>Fitxa del llibre</h1>
    {% if llibre %}
    <ul>
        <li>Titol: {{llibre.titol}}</li>
        <li>Autor: {{llibre.autor}}</li>
        <li>Pàgines: {{llibre.pagines}}</li>
    </ul>
    {% else %}
    <img class="imatge" src="{{ asset('img/lupa.png') }}" alt="libros">
    {% endif %}
    <a href="{{path('inici')}}">Torna a l'inici</a><img style="width: 50px" class="imatge" src="{{ asset('img/flecha.png') }}" alt="libros">
{% endblock %}
